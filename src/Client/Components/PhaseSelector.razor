@inherits PhaseSelectorBase

<MudAutocomplete T="Phase" Label="Phases" @bind-Value="SelectedPhase" SearchFunc="@(m => SearchPhase(m))"  ToStringFunc="@(x => x.Name)" Margin="Margin.Dense" Dense="true"/>
